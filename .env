# Environment variables must be prefixed with VITE_ to be exposed to client code
# via import.meta.env. No secrets live in this file; that is why it is safe to
# keep in the repo for this training project.
VITE_APP_TITLE=Evergreen Insurance - Get a Quote
